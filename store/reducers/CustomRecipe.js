const initialState = {
    id: 0, 
    title: '', 
    imageUri: '', 
    username: '',
};

const CustomRecipeReducer = (state = initialState, action) => {
    return state;
}

export default CustomRecipeReducer; 